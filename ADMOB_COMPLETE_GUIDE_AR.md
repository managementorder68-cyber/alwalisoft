# 📱 دليل شامل لربط Google AdMob بالتطبيق والبوت

## 📋 جدول المحتويات

1. [إنشاء حساب Google AdMob](#1-إنشاء-حساب-google-admob)
2. [إنشاء التطبيق وإعداداته](#2-إنشاء-التطبيق-وإعداداته)
3. [إنشاء الوحدات الإعلانية](#3-إنشاء-الوحدات-الإعلانية)
4. [ربط AdMob بالتطبيق](#4-ربط-admob-بالتطبيق)
5. [ربط AdMob بالبوت](#5-ربط-admob-بالبوت)
6. [إنشاء صفحات الإعلانات](#6-إنشاء-صفحات-الإعلانات)
7. [API Endpoints للإعلانات](#7-api-endpoints-للإعلانات)
8. [التتبع والتحليلات](#8-التتبع-والتحليلات)
9. [الاختبار](#9-الاختبار)
10. [النشر والإطلاق](#10-النشر-والإطلاق)

---

## 1. إنشاء حساب Google AdMob

### الخطوة 1: التسجيل
```
1. اذهب إلى: https://admob.google.com
2. انقر "البدء"
3. سجل الدخول بحساب Google
4. املأ معلومات حسابك:
   - الاسم
   - البلد
   - العملة (USD موصى بها)
   - المنطقة الزمنية
```

### الخطوة 2: قبول الشروط
```
✓ اقرأ وافق على شروط الخدمة
✓ أكمل معلومات الدفع (ستحتاجها لاستلام الأرباح)
```

### الخطوة 3: التحقق من الحساب
```
- سيرسل Google رمز تحقق لبريدك
- أدخل الرمز للتأكيد
- احتفظ بمعلومات الحساب في مكان آمن
```

---

## 2. إنشاء التطبيق وإعداداته

### الخطوة 1: إضافة تطبيق جديد

في لوحة تحكم AdMob:

```
1. اذهب إلى "التطبيقات" → "إضافة تطبيق"

2. اختر:
   □ نظام التشغيل: Android / iOS / Web
   ☑ هل تطبيقك منشور على Google Play أو App Store؟
      → اختر "لا" إذا كان تطبيق ويب

3. معلومات التطبيق:
   - اسم التطبيق: "AlWaliSoft Rewards"
   - النظام الأساسي: "ويب" أو "أندرويد"
   - الفئة: "Entertainment" أو "Productivity"
```

### الخطوة 2: الحصول على App ID

بعد إنشاء التطبيق، ستحصل على:
```
App ID: ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY

مثال:
ca-app-pub-3940256099942544~3347511713
```

**احفظ هذا الـ App ID!** ستحتاجه في `.env`

---

## 3. إنشاء الوحدات الإعلانية

### أنواع الإعلانات المطلوبة:

#### 1. إعلان الفيديو بمكافأة (Rewarded Video)

```
في AdMob Dashboard:
1. اذهب إلى "وحدات الإعلانات" → "إنشاء وحدة إعلانية"
2. اختر "إعلان فيديو بمكافأة"
3. املأ التفاصيل:
   - الاسم: "Video Reward 500 Coins"
   - المكافأة: 500 عملة
   - الوصف: "مشاهدة فيديو للحصول على 500 عملة"
4. احفظ

ستحصل على Ad Unit ID:
ca-app-pub-XXXXXXXXXXXXXXXX/ZZZZZZZZZZ

مثال:
ca-app-pub-3940256099942544/5224354917
```

#### 2. إعلان بيني (Interstitial)

```
1. اذهب إلى "وحدات الإعلانات" → "إنشاء وحدة إعلانية"
2. اختر "إعلان بيني"
3. املأ التفاصيل:
   - الاسم: "Interstitial Between Pages"
   - الوصف: "إعلان ملء الشاشة بين الصفحات"
4. احفظ

ستحصل على Ad Unit ID:
ca-app-pub-XXXXXXXXXXXXXXXX/WWWWWWWWWW

مثال:
ca-app-pub-3940256099942544/1033173712
```

#### 3. إعلان بانر (Banner)

```
1. اذهب إلى "وحدات الإعلانات" → "إنشاء وحدة إعلانية"
2. اختر "بانر"
3. املأ التفاصيل:
   - الاسم: "Bottom Banner"
   - الحجم: "بانر قياسي (320x50)" أو "بانر كبير (320x100)"
4. احفظ

ستحصل على Ad Unit ID:
ca-app-pub-XXXXXXXXXXXXXXXX/VVVVVVVVVV

مثال:
ca-app-pub-3940256099942544/6300978111
```

---

## 4. ربط AdMob بالتطبيق

### الخطوة 1: إضافة المعرفات لـ `.env`

أنشئ ملف `.env.local` أو حدّث `.env`:

```bash
# Google AdMob Configuration
NEXT_PUBLIC_ADMOB_APP_ID=ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY
NEXT_PUBLIC_ADMOB_REWARDED_VIDEO_ID=ca-app-pub-XXXXXXXXXXXXXXXX/ZZZZZZZZZZ
NEXT_PUBLIC_ADMOB_INTERSTITIAL_ID=ca-app-pub-XXXXXXXXXXXXXXXX/WWWWWWWWWW
NEXT_PUBLIC_ADMOB_BANNER_ID=ca-app-pub-XXXXXXXXXXXXXXXX/VVVVVVVVVV

# Ad Settings
NEXT_PUBLIC_AD_DAILY_LIMIT=10
NEXT_PUBLIC_AD_REWARD_AMOUNT=500
```

### الخطوة 2: تحديث ملف الـ `.env.example`

```bash
# إضافة للـ .env.example للوثائق
NEXT_PUBLIC_ADMOB_APP_ID=your_admob_app_id
NEXT_PUBLIC_ADMOB_REWARDED_VIDEO_ID=your_rewarded_video_id
NEXT_PUBLIC_ADMOB_INTERSTITIAL_ID=your_interstitial_id
NEXT_PUBLIC_ADMOB_BANNER_ID=your_banner_id
```

### الخطوة 3: التحقق من النظام الموجود

الملف `lib/ad-manager.ts` جاهز ويقرأ من `.env` تلقائياً!

```typescript
this.config = {
  appId: process.env.NEXT_PUBLIC_ADMOB_APP_ID || '',
  rewardedVideoId: process.env.NEXT_PUBLIC_ADMOB_REWARDED_VIDEO_ID || '',
  interstitialId: process.env.NEXT_PUBLIC_ADMOB_INTERSTITIAL_ID || '',
  bannerId: process.env.NEXT_PUBLIC_ADMOB_BANNER_ID || ''
};
```

✅ **النظام جاهز للعمل!**

---

## 5. ربط AdMob بالبوت

### الخطوة 1: إضافة معرفات للبوت

في ملف `.env` (نفس الملف):

```bash
# Bot Configuration (نفس المعرفات)
ADMOB_APP_ID=ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY
ADMOB_REWARDED_VIDEO_ID=ca-app-pub-XXXXXXXXXXXXXXXX/ZZZZZZZZZZ
```

### الخطوة 2: تعديل كود البوت (إذا لزم الأمر)

البوت عادة لا يعرض إعلانات مباشرة، لكن يمكنه:
1. إرسال رابط للتطبيق لمشاهدة إعلان
2. تتبع إحصائيات الإعلانات

**مثال - إضافة أمر لمشاهدة إعلان:**

```typescript
// bot/handlers/ads.ts
import { Composer } from 'telegraf';

export const adsHandler = new Composer();

adsHandler.command('watch_ad', async (ctx) => {
  const userId = ctx.from.id;
  const webAppUrl = process.env.NEXT_PUBLIC_WEB_APP_URL;
  
  await ctx.reply(
    '📺 شاهد إعلاناً واحصل على 500 عملة!',
    {
      reply_markup: {
        inline_keyboard: [[
          {
            text: '🎬 شاهد الإعلان الآن',
            web_app: { url: `${webAppUrl}/mini-app/ads/watch` }
          }
        ]]
      }
    }
  );
});
```

---

## 6. إنشاء صفحات الإعلانات

### صفحة مشاهدة إعلان الفيديو

سأنشئها لك الآن!

---

## 7. API Endpoints للإعلانات

### الـ API الموجودة حالياً:

✅ الملف موجود: `/app/api/ads/watch/route.ts`

سنتأكد من أنه محدث ويعمل بشكل صحيح.

---

## 8. التتبع والتحليلات

### في لوحة AdMob:

```
1. اذهب إلى "التقارير"
2. ستجد:
   - الأرباح اليومية
   - مرات الظهور (Impressions)
   - النقرات (Clicks)
   - نسبة النقر إلى الظهور (CTR)
   - تكلفة الألف ظهور (eCPM)
   - معدل الإكمال (للفيديو)
```

### في قاعدة البيانات:

جدول `AdWatch` يتتبع:
- من شاهد الإعلان
- متى شاهده
- نوع الإعلان
- المكافأة المستلمة

جدول `AdRevenue` يتتبع:
- الإيرادات اليومية
- عدد الإعلانات
- الإحصائيات

---

## 9. الاختبار

### أثناء التطوير - استخدم Test IDs من Google:

```bash
# في .env.local (للتطوير فقط)
NEXT_PUBLIC_ADMOB_APP_ID=ca-app-pub-3940256099942544~3347511713
NEXT_PUBLIC_ADMOB_REWARDED_VIDEO_ID=ca-app-pub-3940256099942544/5224354917
NEXT_PUBLIC_ADMOB_INTERSTITIAL_ID=ca-app-pub-3940256099942544/1033173712
NEXT_PUBLIC_ADMOB_BANNER_ID=ca-app-pub-3940256099942544/6300978111
```

⚠️ **هام:** هذه معرفات اختبار من Google. **لا تستخدمها في الإنتاج!**

### طريقة الاختبار:

1. افتح التطبيق في المتصفح
2. اذهب لصفحة الإعلانات
3. انقر "شاهد إعلان"
4. يجب أن يظهر إعلان اختباري
5. أكمل الإعلان
6. تحقق من إضافة 500 عملة

---

## 10. النشر والإطلاق

### قبل النشر:

```
☑ استبدل Test IDs بالمعرفات الحقيقية
☑ تأكد من تحديث .env.production
☑ اختبر على جهاز حقيقي
☑ تحقق من سياسة AdMob (لا محتوى محظور)
☑ راجع شروط استخدام AdMob
```

### في `.env.production`:

```bash
# Production AdMob IDs (الحقيقية)
NEXT_PUBLIC_ADMOB_APP_ID=ca-app-pub-YOUR_REAL_APP_ID
NEXT_PUBLIC_ADMOB_REWARDED_VIDEO_ID=ca-app-pub-YOUR_REAL_REWARDED_ID
NEXT_PUBLIC_ADMOB_INTERSTITIAL_ID=ca-app-pub-YOUR_REAL_INTERSTITIAL_ID
NEXT_PUBLIC_ADMOB_BANNER_ID=ca-app-pub-YOUR_REAL_BANNER_ID
```

### في Vercel:

```
1. اذهب لـ Vercel Dashboard
2. اختر مشروعك
3. Settings → Environment Variables
4. أضف كل المتغيرات:
   NEXT_PUBLIC_ADMOB_APP_ID = ca-app-pub-...
   NEXT_PUBLIC_ADMOB_REWARDED_VIDEO_ID = ca-app-pub-...
   ...
5. احفظ
6. أعد نشر التطبيق (Redeploy)
```

---

## 📊 هيكل نظام الإعلانات الحالي

### قاعدة البيانات (Prisma Schema):

```prisma
model AdWatch {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  adType      String   // REWARDED_VIDEO, INTERSTITIAL, BANNER
  adUnitId    String
  reward      Int      @default(0)
  completed   Boolean  @default(false)
  watchedAt   DateTime @default(now())
  
  @@index([userId])
  @@index([watchedAt])
}

model AdRevenue {
  id          String   @id @default(cuid())
  date        DateTime @unique
  adType      String
  impressions Int      @default(0)
  clicks      Int      @default(0)
  revenue     Float    @default(0)
  eCPM        Float    @default(0)
  createdAt   DateTime @default(now())
  
  @@unique([date, adType])
  @@index([date])
}
```

### الملفات الموجودة:

```
✅ lib/ad-manager.ts          - إدارة الإعلانات
✅ components/rewarded-ad-button.tsx - زر الإعلان
✅ app/api/ads/watch/route.ts - API مشاهدة إعلان
✅ app/api/ads/stats/route.ts - API إحصائيات
✅ app/admin/ads/page.tsx     - صفحة الأدمن
```

---

## 🎯 الخطوات التالية

الآن سأقوم بـ:

1. ✅ إنشاء صفحة مشاهدة الإعلانات للمستخدمين
2. ✅ التحقق من API endpoints
3. ✅ إنشاء دليل اختبار شامل
4. ✅ إضافة تتبع تفصيلي

**جاهز؟ دعني أبدأ بإنشاء الصفحات! 🚀**

---

## 💡 نصائح مهمة

### زيادة الأرباح:
```
1. ضع إعلانات في نقاط استراتيجية:
   - بعد إكمال مهمة
   - قبل فتح صندوق مكافأة
   - عند الانتقال بين الصفحات المهمة

2. قدم مكافآت جذابة:
   - 500-1000 عملة للفيديو
   - عملات مضاعفة
   - وصول لميزات خاصة

3. لا تزعج المستخدمين:
   - حد أقصى 10 فيديوهات يومياً
   - خيار تخطي بعد 5 ثوان
   - لا إعلانات إجبارية
```

### الامتثال لسياسات AdMob:
```
✓ لا محتوى للبالغين
✓ لا تضليل للمستخدمين
✓ لا نقرات زائفة
✓ شفافية في المكافآت
✓ تجربة مستخدم جيدة
```

---

**هذا هو الدليل الشامل! الآن سأبدأ بإنشاء الصفحات والكود المطلوب... 🎬**
